import {View, Text} from 'react-native';
import { shared } from '../styles/sharedSheet';

export default function Signup({navigation})
{
    return <View style={shared.container}><Text>Sign Up</Text></View>
}